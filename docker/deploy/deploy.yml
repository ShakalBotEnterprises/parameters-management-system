version: "3.8"

services:
  pms:
    image: ${PMS_IMAGE}:${TAG:-latest}
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: any
        delay: 5s
        max_attempts: 3
        window: 120s
      resources:
        limits:
          cpus: ${PMS_LIMITS_CPU}
          memory: ${PMS_LIMITS_MEM}
        reservations:
          cpus: ${PMS_REQ_CPU}
          memory: ${PMS_REQ_MEM}
    stop_grace_period: 60s
    healthcheck:
      test: [ "CMD-SHELL", "curl --fail --silent http://localhost:8080/actuator/health || exit 1" ]
      interval: 5s
      timeout: 10s
      retries: 48
    env_file:
      - .env
    networks:
      - cluster_network
    volumes:
      - ${NFS}/services/pms-server/volumes/pms-server:/volumes/pms-server
    logging:
      driver: "json-file"
      options:
        max-size: "15m"
        max-file: 1

networks:
  cluster_network:
    external: true
    name: ${CLUSTER_NETWORK}
